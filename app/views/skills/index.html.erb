<div class="welcome">
  <h4>Hello, <%= current_user.first_name %> ! What do you need fixed today?</h4>
</div>
<div class="container text-center my-3">
  <!-- break -->
  <%= form_tag skills_path, method: :get do %>
    <div class="search-form-control form-group">
      <%= text_field_tag :query,
    params[:query],
    class: "form-control",
    placeholder: "Select a skill"
  %>
      <button name="button" type="submit" class="btn btn-flat">
        <i class="fas fa-search"></i> Search
      </button>
    <% end %>
  </div>
  <!-- Skills Listings -->
  <% @skills.each do |skill| %>
    <div class="card-skill">
      <div class="card-category" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url('<%= image_path skill.category.image %>')">
        <%= skill.category.title %>
      </div>
      <div class="card-skill-infos">
        <div class="col">
          <h4><%= skill.title %></h4>
          <h6><%= skill.user.first_name %></h6>
          <p><i class="fas fa-euro-sign"></i><%= skill.price_per_hour %>/per hour</p>
          <p><%= skill.user.location %></p>
        </div>
        <%= image_tag skill.user.picture, class:"card-skill-user avatar-bordered"%>
        <%= link_to "See More", skill_path(skill), class:"button-more" %>
      </div>
  </div>